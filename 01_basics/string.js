const name = "ashish"
const repoCount = 50

// console.log(name + repoCount + " value")


console.log(`hello my name is ${name} amd my repo count is ${repoCount}`);

const gameName = new String("ashishyadav")

console.log(gameName[5]);
console.log(gameName.toUpperCase());

CharAt (can check the string which sentence is there at any number
 )
indexof (can the number at which the letters are going to be)

const newgameName = gameName.substring(0, 2)
console.log(newst);

trim
replace
