// dates in javascript

let mydate = new Date()
console.log(myDate.tostring());

to datestring
to isosting
to JSON
to localdatestring
to localtimestring
to utcstring

date is an object.

let myCreatedDate = new Date(2025, 12, 22)
console.log(myCreatedDate);
